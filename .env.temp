# Temporary PostgreSQL configuration for testing
# This simulates the Vercel environment locally
DATABASE_URL="postgresql://postgres:password@localhost:5432/vidyawebbuilder"

# Fallback to local SQLite if PostgreSQL is not available
# DATABASE_URL="file:./dev.db"